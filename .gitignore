# WireGuard secrets (copy from wireguard-secrets.nix.example)
**/wireguard-secrets.nix

# NFS mount secrets (copy from nfs-secrets.nix.example)
**/nfs-secrets.nix


nixos/baal/hardware-configuration.nix