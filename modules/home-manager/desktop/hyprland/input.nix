# Hyprland input (from omanix)
_: {
  wayland.windowManager.hyprland.settings = {
    input = {
      kb_layout = "us";
      follow_mouse = 1;
      numlock_by_default = true;
    };
    gesture = [
      "3, horizontal, workspace"
    ];
  };
}
